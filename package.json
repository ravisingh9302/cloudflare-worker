{
  "name": "serverless-api",
  "version": "0.0.0",
  "devDependencies": {
    "vitest": "^0.34.6",
    "wrangler": "3.14.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  }
}
